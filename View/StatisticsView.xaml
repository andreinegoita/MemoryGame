<UserControl x:Class="MemoryGame.View.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemoryGame.View"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="950">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Source="/Assets/MemoryGamePlayMenu.png" Stretch="Fill" Grid.RowSpan="3"/>

        <TextBlock Grid.Row="0" Text="Player Statistics" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,20,0,10" Foreground="White"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding PlayerStats}" AutoGenerateColumns="False" 
                  Background="#80000000"  Foreground="Black" BorderThickness="1" 
                  BorderBrush="Gray" GridLinesVisibility="All"
                  HorizontalAlignment="Center" VerticalAlignment="Center" 
                  Width="700" Height="400" Margin="0,0,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Player Name" Foreground="Black" Binding="{Binding PlayerName}" Width="*"/>
                <DataGridTextColumn Header="Games Played" Foreground="Black" Binding="{Binding GamesPlayed}" Width="*"/>
                <DataGridTextColumn Header="Games Won"  Foreground="Black" Binding="{Binding GamesWon}" Width="*"/>
                <DataGridTextColumn Header="Win Rate (%)"  Foreground="Black" Binding="{Binding WinRate, StringFormat={}{0:N1}%}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Back" Command="{Binding BackCommand}" 
                Width="150" Height="40" Margin="0,20,0,30"/>
    </Grid>
</UserControl>