<Window x:Class="MemoryGame.View.LoadGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MemoryGame.View"
        Title="Load Game" Height="300" Width="400"
        Background="#F5F5F5" BorderBrush="#FF009688" BorderThickness="2" WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <Style x:Key="ModernListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF009688"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid Margin="10" Background="White">
                            <TextBlock Text="{Binding SaveDate}" FontWeight="Bold" Foreground="#FF004D40" VerticalAlignment="Center" Margin="10"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="5" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF009688"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#FF009688"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="5" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>

        <ListBox Name="SavedGamesListBox" 
                 ItemsSource="{Binding SavedGames}" 
                 SelectedItem="{Binding SelectedGame, Mode=TwoWay}"
                 DisplayMemberPath="SaveDate" 
                 Style="{StaticResource ModernListBoxStyle}" 
                 Margin="20,20,20,80"/>


        <Button Content="Load" Command="{Binding LoadGameCommand}" 
                Style="{StaticResource ModernButtonStyle}" 
                HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                Margin="0,0,0,20"/>

        <Button Content="Cancel" Command="{Binding CancelCommand}" 
                Style="{StaticResource ModernButtonStyle}" 
                HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                Margin="0,0,0,60"/>
    </Grid>
</Window>
