<UserControl x:Class="MemoryGame.View.AboutWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemoryGame.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#6200EE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                         BorderBrush="{TemplateBinding BorderBrush}" 
                         BorderThickness="{TemplateBinding BorderThickness}" 
                         CornerRadius="25">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Image Source="/Assets/MemoryGamePlayMenu.png" Stretch="Fill" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="#80000000" Margin="20">
            <TextBlock FontSize="24" FontWeight="Bold" Foreground="White" Text="Student: Negoiță Andrei" />
            <TextBlock FontSize="18" Foreground="White" Text="Specializare: Informatică" />
            <TextBlock FontSize="18" Foreground="White" Text="Grupa: 10LF233" />
            <TextBlock FontSize="18" Foreground="White">
                <Hyperlink Command="{Binding OpenEmailCommand}">
                    Email: andrei.negoita@student.unitbv.ro
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Button Content="Exit" Command="{Binding ExitCommand}" Style="{StaticResource ModernButtonStyle}" Height="40" Width="150" Margin="325,317,325,93"/>
    </Grid>
</UserControl>
